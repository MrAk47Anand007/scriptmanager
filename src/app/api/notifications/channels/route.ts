import { NextResponse } from 'next/server';import { prisma } from '@/lib/db'
export async function GET(){return NextResponse.json(await prisma.notificationChannel.findMany({include:{_count:{select:{rules:true,deliveries:true}}},orderBy:{createdAt:'desc'}}))}
export async function POST(request:Request){const body=await request.json() as {name:string;kind:string;config?:unknown};if(!['desktop','webhook','slack','smtp','teams'].includes(body.kind))return NextResponse.json({error:'Invalid channel'},{status:400});return NextResponse.json(await prisma.notificationChannel.create({data:{name:body.name,kind:body.kind,configJson:JSON.stringify(body.config??{})}}),{status:201})}
